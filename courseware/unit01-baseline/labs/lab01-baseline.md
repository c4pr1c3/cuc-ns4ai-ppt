# Lab 01: 安全基线（capstone M1）

> 📝 大纲。在 M0 威胁建模基础上，把基线安全落到 `capstone/seed/app.py`（当前是弱实现：明文口令、无失败计数）。

## 1. 实验目标
- 能力交付（簇①·L3）：设计并实现鉴权/会话/RBAC + 网络基线。

## 2. 环境准备
- fork `capstone/seed/`，跑起 `app.py`（127.0.0.1:5000）。

## 3. 任务清单（计划）
- 任务 A：口令哈希存储 + 登录失败计数/锁定
- 任务 B：会话管理（安全 cookie、过期、防固定）
- 任务 C：RBAC 授权（最小权限，区分 admin/alice）
- 任务 D：网络/传输基线（暴露面收敛、HTTPS 思路）

## 4. 交付与量规
- 绑定簇①·L3；tag `m1`；附「相对 seed 弱实现的加固对照」。

## 5. 能力自评
- 簇①·L3：RBAC/风险登记随项目演进 ✅
